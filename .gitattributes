# Normalise line endings in the repository, while each machine keeps its own.
#
# This repository is the mod's only one. It was published by `git subtree push` from a monorepo
# until 2026-09-12, which is why this file used to explain that the monorepo kept a copy of it;
# the mod has its own repository now, and nothing exports this folder from anywhere else.

* text=auto

*.png binary
*.dll binary
